# Public domain pointed at this server (A record). Caddy issues TLS for it.
DOMAIN=shelif.co

# Email used by Let's Encrypt for cert notices.
ACME_EMAIL=agdemirhalim4@gmail.com

# Optional: contact form (leave empty to disable until you get a Resend key).
RESEND_API_KEY=
CONTACT_TO_EMAIL=halim@shelif.com
